syntax = "proto3";

package account;

option go_package = ".;account";

message UserCredential {
  // 帐户
  string account = 1;
  // 密码
  string password = 2;
}

message LoginResponse {
  // 帐户
  string accessToken = 1;
}